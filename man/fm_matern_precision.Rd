% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matern.R
\name{fm_matern_precision}
\alias{fm_matern_precision}
\alias{fm_gmrf_sample}
\title{Mat\'ern process precision}
\usage{
fm_matern_precision(x, alpha, rho, sigma)

fm_gmrf_sample(n, Q)
}
\arguments{
\item{x}{A mesh object}

\item{alpha}{The SPDE operator order. The smoothness index is \code{alpha - dim/2}.}

\item{rho}{The range parameter.}

\item{sigma}{The nominal standard deviation.}

\item{n}{The number of samples to generate}

\item{Q}{A precision matrix}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Construct the (sparse) precision matrix for the basis weights for
Whittle-Mat\'ern SPDE models.
}
\section{Functions}{
\itemize{
\item \code{fm_gmrf_sample()}: Generate \code{n} samples based on a sparse precision matrix \code{Q}

}}
\examples{
library(Matrix)
mesh <- fm_mesh_1d((0:10)^2, degree = 2)
Q <- fm_matern_precision(mesh, alpha = 2, rho = 15, sigma = 1)
x <- seq(0, 100, length.out = 301)
A <- fm_basis(mesh, x)
plot(x, as.vector(Matrix::diag(A \%*\% solve(Q, t(A)))), type = "l")

plot(x, fm_evaluate(mesh, loc = x, field = fm_gmrf_sample(1, Q)[, 1]), type = "l")
}
