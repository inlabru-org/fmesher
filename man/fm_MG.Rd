% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_graph.R
\name{fm_MG}
\alias{fm_MG}
\alias{fm_as_MG}
\alias{fm_basis.metric_graph}
\alias{fm_bary.metric_graph}
\alias{fm_bary.fm_MGG}
\alias{fm_bary.fm_MGM}
\alias{fm_bary_simplex.metric_graph}
\alias{fm_bary_simplex.fm_MGG}
\alias{fm_bary_simplex.fm_MGM}
\alias{fm_manifold_get.metric_graph}
\alias{fm_dof.metric_graph}
\alias{fm_dof.fm_MGG}
\alias{fm_dof.fm_MGM}
\alias{fm_int.metric_graph}
\alias{fm_int.fm_MGM}
\alias{fm_int.fm_MGG}
\title{fmesher interface methods for \code{metric_graph} objects}
\usage{
fm_as_MG(x, MGG = NULL)

\method{fm_basis}{metric_graph}(x, loc, weights = NULL, ..., full = FALSE)

\method{fm_bary}{metric_graph}(mesh, loc, ...)

\method{fm_bary}{fm_MGG}(mesh, loc, ...)

\method{fm_bary}{fm_MGM}(mesh, loc, MGG = TRUE, ...)

\method{fm_bary_simplex}{metric_graph}(mesh, bary = NULL, ...)

\method{fm_bary_simplex}{fm_MGG}(mesh, bary = NULL, ...)

\method{fm_bary_simplex}{fm_MGM}(mesh, bary = NULL, ...)

\method{fm_manifold_get}{metric_graph}()

\method{fm_dof}{metric_graph}(x)

\method{fm_dof}{fm_MGG}(x)

\method{fm_dof}{fm_MGM}(x)

\method{fm_int}{metric_graph}(domain, samplers = NULL, name = "x", ...)

\method{fm_int}{fm_MGM}(domain, samplers = NULL, name = "x", ...)

\method{fm_int}{fm_MGG}(domain, samplers = NULL, name = "x", ...)
}
\arguments{
\item{x}{metric_graph object}

\item{MGG}{indicator for the barycentric coordinates related to the graph
(MGG) or mesh (MGM), or NULL. Passed on to \code{\link[=fm_as_MG]{fm_as_MG()}}}

\item{loc}{Observation locations, can be either MGG coordinates, MGM
coordinates or Euclidean coordinates (passed to \code{\link[=fm_bary]{fm_bary()}})}

\item{weights}{Optional weight vector, one weight for each location}

\item{...}{Passed on to submethods}

\item{full}{logical; if \code{TRUE}, return a \code{fm_basis} object, containing at
least a projection matrix \code{A} and logical vector \code{ok} indicating which
evaluations are valid. If \code{FALSE}, return only the projection matrix \code{A}.
Default is \code{FALSE}.}
}
\value{
A \code{fm_basis} object; a list of evaluator information objects,
at least a matrix \code{A} and logical vector \code{ok}.
}
\description{
Interface for \code{metric_graph} objects
}
\section{Functions}{
\itemize{
\item \code{fm_as_MG()}: Associate a \code{metric_graph} object with class \code{fm_MGG} or
\code{fm_MGM}.

\item \code{fm_basis(metric_graph)}: Construct an interpolation/basis matrix

\item \code{fm_bary(metric_graph)}: Compute an \code{fm_bary_MGG} or \code{fm_bary_MGM} object

\item \code{fm_bary(fm_MGG)}: Compute and \code{fm_bary_MGG} object

\item \code{fm_bary(fm_MGM)}: Compute and \code{fm_bary_MGM} object

\item \code{fm_bary_simplex(metric_graph)}: Returns a \code{metric_graph} graph or mesh edge index matrix

\item \code{fm_bary_simplex(fm_MGG)}: Returns a \code{metric_graph} graph edge index matrix

\item \code{fm_bary_simplex(fm_MGM)}: Returns a \code{metric_graph} mesh edge index matrix

\item \code{fm_manifold_get(metric_graph)}: Return manifold type, always "G1" for \code{metric_graph}

\item \code{fm_dof(metric_graph)}: Returns the degrees of freedom (number of vertices in the
graph or mesh, depending on the object). \code{fm_as_MG(x)} is used to ensure
\code{fm_MGG} or \code{fm_MGM} class.

\item \code{fm_dof(fm_MGG)}: \code{fm_MGG} method for \code{fm_dof()}

\item \code{fm_dof(fm_MGM)}: \code{fm_MGM} method for \code{fm_dof()}

\item \code{fm_int(metric_graph)}: \code{metric_graph} integration. Supported samplers:
\itemize{
\item \code{NULL} for integration over the entire domain;
\item A tibble with a named column containing a matrix with single edge
intervals (ordered), and optionally a \code{weight} column.
}

\item \code{fm_int(fm_MGM)}: Integrate on an \code{fm_MGM} object

\item \code{fm_int(fm_MGG)}: Integration on \code{metric_graph}; requires a mesh in the graph.

}}
\examples{
if (requireNamespace("MetricGraph")) {
  edge1 <- rbind(c(0, 0), c(1, 0))
  edge2 <- rbind(c(0, 0), c(0, 1))
  edge3 <- rbind(c(0, 1), c(-1, 1))
  theta <- seq(from = pi, to = 3 * pi / 2, length.out = 20)
  edge4 <- cbind(sin(theta), 1 + cos(theta))
  edges <- list(edge1, edge2, edge3, edge4)
  graph <- MetricGraph::metric_graph$new(edges = edges)
  graph$build_mesh(h = 0.01)
  p1 <- simple_path_MGG(
    graph = graph,
    start_MGG = cbind(1, 0.2),
    edges = c(2),
    end_MGG = cbind(3, 0.8)
  )
  samplers <- tibble::tibble(x = list(p1), weight = c(1))
  ips <- fm_int(
    graph,
    samplers
  )
}
}
